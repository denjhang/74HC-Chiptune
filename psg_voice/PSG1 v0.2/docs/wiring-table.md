# PSG1 v0.2 接线表

> **v0.1 + 4-bit 音量（TLC7524 衰减器）**，去掉 gate，toggle_q 直接经 TLC7524 衰减输出。
> 基于 v0.1 接线表，标注 **改动点**（加 HC374 + TLC7524，去 HC00 的 gate 与门）。

## 芯片位置

| 位号 | 型号 | 功能 | vs v0.1 |
|------|------|------|---------|
| U0 | 74LS373 | 电平转换层（FT232H 3.3V→5V）| 复用 |
| U1 | 74HC373 | period 锁存 | 复用 |
| U2 | 74HC161 | 计数器低 4 位 | 复用 |
| U3 | 74HC161 | 计数器高 4 位 | 复用 |
| U4 | 74HC00 | PE 反相（仅第 1 路，**gate 与门拆除**）| **改动：去 gate** |
| U5 | 74HCT74 | toggle 翻转 | 复用 |
| **U6** | **74HC374** | **音量码锁存（D0-D3，A0 选通）**| **新增** |
| **U7** | **TLC7524** | **音量衰减器（R-2R 正向接法：REF 输入/OUT1 输出）**| **新增** |
| U8 | 喇叭 | 接 OUT1（PSG_OUT），经耦合电容+运放 | 改接 U7（原接 U4） |

## 总线信号（去 gate，加 A0）

| 信号 | 来源 | 去向 | vs v0.1 |
|------|------|------|---------|
| `clk` | 外部时钟 64kHz | U2.P2, U3.P2, U5.P3 | 不变 |
| `rst_n` | FT232H D6 (经 LS373) | U2.P1, U3.P1, U5.P1, U5.P13 | 不变 |
| `period_le` | FT232H D4 (经 LS373) | U1.P11（HC373 LE）| 不变 |
| **`A0`** | **FT232H D5 (经 LS373)** | **U6.P11（HC374 CP）**| **v0.1 是 gate，现改 A0** |
| `period_in[7:0]` | FT232H C0-C7 (经 LS373) | U1.D0-D7 | 不变 |
| `toggle_q` | U5.P9 | **U7.P15（TLC7524 Pin15 / REF，方波参考输入）** | **改：原接喇叭→接 TLC7524 REF** |
| `PSG_OUT` | U7.P1（TLC7524 OUT1）| → 耦合电容 → 运放 → 喇叭 | 新增（衰减后方波输出）|
| `vol[3:0]` | U6.Q0-Q3 | U7.DB4-DB7 | 新增 |

> **gate 去除**：v0.1 的 HC00 第 2/3 路（gate 与门）拆除。toggle_q 不再经 gate，直接输出。
> **静音方式**：写 vol=0000 → TLC7524 输出 0 → 静音（替代 gate）。

---

## U6 — 74HC374（音量码锁存，新增）

8 位 D 触发器，CP 上升沿锁存。只用了低 4 位（D0-D3），高 4 位接地。

| Pin | 信号 | 连接 |
|-----|------|------|
| 1 | /OE | → GND（常输出）|
| 11 | CP | ← **A0**（FT232H D5 经 LS373，写脉冲）|
| 9 | D0 | ← FT232H D0（经 LS373，音量 bit0）|
| 8 | D1 | ← FT232H D1（经 LS373，音量 bit1）|
| 7 | D2 | ← FT232H D2（经 LS373，音量 bit2）|
| 6 | D3 | ← FT232H D3（经 LS373，音量 bit3）|
| 12-15 | D4-D7 | → GND（不用，音量只用 4 位）|
| 2 | Q0 | → U7.P7（TLC7524 Pin7 / DB4）|
| 3 | Q1 | → U7.P6（TLC7524 Pin6 / DB5）|
| 4 | Q2 | → U7.P5（TLC7524 Pin5 / DB6）|
| 5 | Q3 | → U7.P4（TLC7524 Pin4 / DB7）|
| 16-19 | Q4-Q7 | 悬空（不用）|
| 20 | VCC | +5V |
| 10 | GND | GND |

> **音量接 DB4-DB7 高 4 位（最优接法）**：
> - **必须连续 4 位**：vol 的 4 位权值须为 1:2:4:8，16 档才等距铺开（每档差 16），无碰撞、无浪费。非连续位（如 DB7,DB5,DB3,DB1）权值跳跃，16 个值会挤成 4 团，实际只有 4 档有效，反而浪费位数。
> - **必须接高段**：DB4-7 → 满量程 D=240（94% 幅度，4.69V），输出幅度和信噪比最大。接 DB0-3 只有 5.9%（0.29V），接 DB3-6 只有 47%，都要额外放大。
> - 即"连续"保不浪费、"高段"保幅度，DB4-7 是 4 位硬件 DAC 的最优解。
> - 4 位硬件 DAC 物理上限即线性 16 档；要 SN/AY 那种对数曲线需软件查表 + DAC 接满 8 位，属另一套架构，留待后续。
>
> **写时序**：FT232H 把 vol 放 D0-D3，A0 给上升沿，HC374 锁存。

---

## U7 — TLC7524（音量衰减器，R-2R 正向接法，新增）

8-bit 乘法 DAC（CMOS R-2R 梯形），16 脚 DIP/SOIC 封装。
**正向接法（R-2R 电压输出，Gigatron 风格）**：REF 当方波输入、OUT1 当输出、RFB 接 GND 补全梯形网络、OUT2 接地。

> ⚠️ **历史教训**：最初误用"反接电压模式"（OUT1=输入/REF=输出/RFB 悬空），实测噪音叠加方波 + 音量失效。
> 原因：①datasheet 电压模式只保证 OUT1≤2.5V 线性，0/5V 方波超范围；②OUT1/REF 寄生电容随数字码跳变（30↔120pF）→ 振铃噪音；③RFB 悬空致梯形网络电流无处泄放。
> 改成正向接法后音量逻辑恢复。详见 handoff.md。

> 引脚定义（实测，DIP-16 俯视，左列 Pin1-8 / 右列 Pin16-9）：

| Pin | 名称 | 信号 | 连接 | 说明 |
|-----|------|------|------|------|
| 1 | OUT1 | **→ U7.P2（喇叭）** | **衰减后的方波输出（PSG_OUT）** |
| 2 | OUT2 | → GND | GND | 互补端接地 |
| 3 | GND | → GND | GND | |
| 4 | DB7 | ← U6.Q3 | 音量 bit3（MSB）| |
| 5 | DB6 | ← U6.Q2 | 音量 bit2 | |
| 6 | DB5 | ← U6.Q1 | 音量 bit1 | |
| 7 | DB4 | ← U6.Q0 | 音量 bit0 | |
| 8 | DB3 | → GND | 固定 0 | |
| 9 | DB2 | → GND | 固定 0 | |
| 10 | DB1 | → GND | 固定 0 | |
| 11 | DB0 | → GND | 固定 0（LSB）| |
| 12 | CS | → GND | GND | 常选通（直通模式）|
| 13 | WR | → GND | GND | 常写入（音量码实时跟随）|
| 14 | VDD | → +5V | 电源 | |
| 15 | REF | ← **U5.P9（toggle_q 方波）** | **参考输入（被衰减的信号）** |
| 16 | RFB | → GND | GND | 接地补全 R-2R 梯形网络 |

> **衰减公式**：OUT1 输出幅度 = VREF × (D/256)
> - toggle_q（0/5V 方波）加在 REF，OUT1 输出 = 5V × (D/256) 的方波
> - D = vol<<4（vol 0-15 → D = 0-240，因 DB0-DB3 接地固定 0）
> - vol=15 → D=240 → 输出 4.69V 方波（满音量）
> - vol=0 → D=0 → 输出 0V（静音）
>
> **OUT1 输出阻抗较高**（R-2R 网络），实际音频链路应经耦合电容 + 运放缓冲再推喇叭
> （datasheet Figure 3 的标准做法是 RFB 接运放输出构成 I-V 闭环；当前 RFB 接 GND 是简化验证）。

---

## U4 改动（HC00 去 gate）

v0.1 的 HC00 第 2/3 路（gate 与门）拆除，仅保留第 1 路（PE 反相）：

| Pin | v0.1 | v0.2 |
|-----|------|------|
| 4 (A2) | ← gate | → GND（不用）|
| 5 (B2) | ← toggle_q | 悬空 |
| 6 (Y2) | → gate_nand1 | 悬空 |
| 8 (Y3) | → wave_out → 喇叭 | **悬空**（不再经 gate）|
| 9,10 (A3,B3) | ← gate_nand1 | → GND |

> **toggle_q 直接输出**：U5.P9（toggle_q）不再经 HC00 的 gate 与门，直接接 U7.P1（TLC7524 OUT1）。
> HC00 第 1 路（PE 反相，Pin 1/2/3）保持不变。

---

## 4-bit 音量档位

vol[3:0] → D = vol << 4 → 输出幅度 = 5V × D/256

> 16 档等距铺开（每档 ΔD=16，对应 ≈-1.4dB/档 区间内的线性步进）。这是 4 位硬件 DAC 的物理上限；接法已在 U6 节论证为最优（DB4-7，连续+高段）。

| vol | D 值 | 输出幅度 | 听感 |
|-----|------|---------|------|
| 0000 | 0 | 0V | 静音 |
| 0001 | 16 | 0.31V | 极小 |
| 0100 | 64 | 1.25V | 小声 |
| 1000 | 128 | 2.5V | 中声 |
| 1100 | 192 | 3.75V | 大声 |
| 1111 | 240 | 4.69V | 满音量 |

## 写时序总表

| 操作 | FT232H 动作 |
|------|------------|
| 写 period | D0-D7=period（C口），D4(LE) 上升沿 → HC373 锁存 |
| 写音量 | D0-D3=vol（C口低4位），D5(A0) 上升沿 → HC374 锁存 |
| 静音 | 写 vol=0000 |
| 复位 | D6(RST) 拉低再拉高 |

## 电源（所有芯片）

| 芯片 | VDD | GND |
|------|-----|-----|
| 74LS373 (U0) | Pin 16 | Pin 8 |
| 74HC373 (U1) | Pin 20 | Pin 10 |
| 74HC161 (U2,U3) | Pin 16 | Pin 8 |
| 74HC00 (U4) | Pin 14 | Pin 7 |
| 74HCT74 (U5) | Pin 14 | Pin 7 |
| **74HC374 (U6)** | **Pin 20** | **Pin 10** |
| **TLC7524 (U7)** | **Pin 14** | **Pin 3**（Pin2=OUT2、Pin8-11=DB3-0、Pin12=CS、Pin13=WR、Pin16=RFB 均接 GND）|

> 每个 VDD 就近加 0.1μF 去耦电容。所有 GND 共地。

## 待验证

- [x] ~~TLC7524 反接电压模式~~ → **已弃用**（噪音+音量失效，见 U7 历史教训）
- [x] TLC7524 正向接法（REF=方波输入/OUT1=输出/RFB=GND）：**音量调整初步生效** ✅
- [ ] OUT1 输出经耦合电容+运放缓冲推喇叭（当前 RFB 接 GND 是简化，OUT1 阻抗仍较高）
- [ ] vol=0 是否真静音（馈通泄漏）
- [ ] HC374 A0 选通时序
