#line 1 "main.c"


extern unsigned char REGISTER_IO0;


unsigned char wave[16] = {0,1,2,3,4,5,6,7,8,7,6,5,4,3,2,1};

void main() {
    unsigned long phase = 0;
    unsigned long step  = 0x00100000;
    unsigned char vol   = 64;
    unsigned char sample, out;
    int i;

    for (i = 0; i < 100; i++) {
        phase += step;
        sample = wave[(phase >> 28) & 15];
        out = (sample * vol) >> 8;
 REGISTER_IO0 = out;
    }
    while (1);
}
