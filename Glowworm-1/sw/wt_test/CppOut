#line 1 "main.c"








#line 1 "glow_types.h"





typedef unsigned char      u8;
typedef unsigned short     u16;
typedef unsigned int       u32;
typedef signed char        s8;
typedef signed short       s16;
typedef signed int         s32;

#line 10 "main.c"

extern u8 REGISTER_IO0;


static u16 pos = 0;
static u16 step_val;
static u8  level = 15;
static u8  vol = 31;
static s8  wav[128];


#line 34 "main.c"

void main() {
    u8 i;

    for (i = 0; i < 128; i++) wav[i] = (s8)(i - 64);
    step_val = 0x0800;

 REGISTER_IO0 = 0xAA;
 do { s16 mix = 0; pos += step_val; { u8 idx = (u8)(pos >> 5) & 0x7F; s8 b = wav[idx]; s16 tmp = (s16)b * (s16)(level + 1) * (s16)(vol + 1); tmp >>= 10; mix += tmp; } if (mix > 127) mix = 127; if (mix < -128) mix = -128;REGISTER_IO0 = (u8)(128 + mix); } while(0);
 REGISTER_IO0 = 0xBB;
 do { s16 mix = 0; pos += step_val; { u8 idx = (u8)(pos >> 5) & 0x7F; s8 b = wav[idx]; s16 tmp = (s16)b * (s16)(level + 1) * (s16)(vol + 1); tmp >>= 10; mix += tmp; } if (mix > 127) mix = 127; if (mix < -128) mix = -128;REGISTER_IO0 = (u8)(128 + mix); } while(0);
 REGISTER_IO0 = 0xCC;
 do { s16 mix = 0; pos += step_val; { u8 idx = (u8)(pos >> 5) & 0x7F; s8 b = wav[idx]; s16 tmp = (s16)b * (s16)(level + 1) * (s16)(vol + 1); tmp >>= 10; mix += tmp; } if (mix > 127) mix = 127; if (mix < -128) mix = -128;REGISTER_IO0 = (u8)(128 + mix); } while(0);
 REGISTER_IO0 = 0xDD;
 do { s16 mix = 0; pos += step_val; { u8 idx = (u8)(pos >> 5) & 0x7F; s8 b = wav[idx]; s16 tmp = (s16)b * (s16)(level + 1) * (s16)(vol + 1); tmp >>= 10; mix += tmp; } if (mix > 127) mix = 127; if (mix < -128) mix = -128;REGISTER_IO0 = (u8)(128 + mix); } while(0);
 REGISTER_IO0 = 0xEE;
 do { s16 mix = 0; pos += step_val; { u8 idx = (u8)(pos >> 5) & 0x7F; s8 b = wav[idx]; s16 tmp = (s16)b * (s16)(level + 1) * (s16)(vol + 1); tmp >>= 10; mix += tmp; } if (mix > 127) mix = 127; if (mix < -128) mix = -128;REGISTER_IO0 = (u8)(128 + mix); } while(0);
 REGISTER_IO0 = 0x55;
    while (1);
}
